# Fonte Oculta — Variáveis de Ambiente
# Copie este arquivo para .env e preencha com suas credenciais reais.
# NUNCA faça commit do arquivo .env — ele já está no .gitignore.

# ── OpenAI / GPT Image 2 (geração de imagens e texto) ────────────────────────
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sua_chave_openai_aqui

# ── Anthropic / Claude AI ─────────────────────────────────────────────────────
# Obtenha em: https://console.anthropic.com/
ANTHROPIC_API_KEY=sua_chave_anthropic_aqui

# ── Gemini AI ─────────────────────────────────────────────────────────────────
# Obtenha em: https://aistudio.google.com/apikey
GEMINI_API_KEY=sua_chave_gemini_aqui

# ── Meta / Instagram Graph API ────────────────────────────────────────────────
# Gere em: https://developers.facebook.com → seu app → Graph API Explorer
META_ACCESS_TOKEN=seu_token_meta_aqui
INSTAGRAM_ACCOUNT_ID=seu_instagram_account_id_aqui
FACEBOOK_PAGE_ID=seu_facebook_page_id_aqui

# ── Credenciais Instagram (login direto) ──────────────────────────────────────
INSTAGRAM_USERNAME=seu_usuario_instagram
INSTAGRAM_PASSWORD=sua_senha_instagram

# ── Apify (scraping e radar) ──────────────────────────────────────────────────
# Obtenha em: https://console.apify.com/
APIFY_API_KEY=sua_chave_apify_aqui

# ── Notion (integração com dashboard/banco de conteúdo) ───────────────────────
# Obtenha em: https://www.notion.so/my-integrations
NOTION_TOKEN=seu_token_notion_aqui

# ── Backblaze B2 (armazenamento de arquivos) ──────────────────────────────────
# Obtenha em: https://www.backblaze.com/b2/docs/
B2_KEY_ID=seu_key_id_b2
B2_APPLICATION_KEY=sua_application_key_b2
B2_BUCKET_NAME=nome_do_seu_bucket
B2_ENDPOINT=https://s3.us-west-000.backblazeb2.com

# ── ElevenLabs (geração de áudio) ─────────────────────────────────────────────
# Obtenha em: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=sua_chave_elevenlabs_aqui

# ── Fal.ai (geração de imagens alternativa) ───────────────────────────────────
# Obtenha em: https://fal.ai/dashboard/keys
FAL_KEY=sua_chave_fal_aqui

# ── Brevo / Sendinblue (envio de emails transacionais e código de verificação) ─
# Obtenha em: https://app.brevo.com/settings/keys/api
BREVO_API_KEY=sua_chave_api_brevo_aqui
BREVO_SENDER_EMAIL=noreply@seudominio.com
BREVO_SENDER_NAME=Oráculo Bella

# ── Dashboard Login ────────────────────────────────────────────────────────────
DASHBOARD_USER=jordao
DASHBOARD_USER_NAME=Super Admin
DASHBOARD_PASS=troque_para_senha_forte
# [OBRIGATÓRIO] Chave secreta para criptografia do token JWT
JWT_SECRET=troque_para_string_aleatoria_longa
# [OBRIGATÓRIO] Chave Pepper secreta do servidor para hash de senhas Argon2id (nunca expor)
PASSWORD_PEPPER=troque_para_string_pepper_secreta_longa
# [OBRIGATÓRIO] Tempo de expiração do token JWT (ex: 24h, 48h, 72h)
JWT_EXPIRES_IN=24h
# [OPCIONAL] Origens permitidas no CORS separadas por vírgula. Se vazio, libera todas. (ex: http://localhost:5176,https://meudominio.com)
CORS_ALLOWED_ORIGINS=http://localhost:5176

# ── Banco de Dados PostgreSQL (pgvector) ──────────────────────────────────────
# [OPCIONAL] Se rodar via Docker Compose, as variáveis padrão já estão prontas.
# Se rodar local, defina os valores adequados.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=123456
DB_NAME=oracle_manager
# [OPCIONAL] Máximo de conexões simultâneas no pool (padrão: 20)
DB_POOL_MAX=20

# ── MinIO (Armazenamento Local S3) ──────────────────────────────────────────
# [OBRIGATÓRIO]
MINIO_ENDPOINT=http://localhost:9000
MINIO_PUBLIC_URL=http://seudominio.com:9000
MINIO_ROOT_USER=oraculo_admin
MINIO_ROOT_PASSWORD=oraculo_secret_123
MINIO_BUCKET=oraculo-bucket

# ── RabbitMQ (Fila de Mensagens / Broker) ─────────────────────────────────────
# [OBRIGATÓRIO]
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=oraculo_user
RABBITMQ_PASS=oraculo_pass_123

# --- Servidor e produção ---
PORT=3131
NODE_ENV=production
CONTENT_WORKSPACE=bella
DASHBOARD_PASS2=

# --- Geração de conteúdo ---
ACTIVE_IMAGE_PROVIDER=gpt-image-2
COPY_GENERATION_MODEL=gpt-5.6-terra
USE_MOCK_GENERATOR=false
# Oráculo único (ligado por padrão): só o Oráculo da Bella escreve ideias e copy.
# Use 0 para voltar ao pipeline antigo com vários agentes.
ORACULO_MODO_UNICO=1

# --- Banco de dados ---
# Com um Postgres acessível (DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME acima) o sistema usa ele.
# Sem Postgres cai no banco embarcado PGlite, que já corrompeu 5 vezes: NÃO use em produção.
# DB_ENGINE=pglite  (só para forçar o embarcado em desenvolvimento)
